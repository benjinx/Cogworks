public class IdleState : State<ExampleState>
{
    // This is used for a blank substate if needed
    public IdleState(ExampleState stateMachine, StateType stateType) : base(stateMachine, stateType)
    {
    }
}
